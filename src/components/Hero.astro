---
---

<section class="hero">
  <div class="container">
    <h1 class="hero-title">
      <span class="hero-line">Hey! I'm James ✌️</span>
    </h1>
    
    <div class="hero-content">
      <p class="hero-description">
        I'm a designer, cyclist, photographer and (sometimes) developer. I'm currently at Pavers working on a range of internal tools and customer facing sites.
      </p>
      <p class="hero-description">
        Prior to Pavers, I worked at coeo, building internal dashboards, crafting marketing content, and designing customer-facing sites. Before that, I completed a degree in Graphic Art & Design at Leeds Beckett University.
      </p>
      
      <div class="hero-buttons">
        <a href="/work" class="button">View Work</a>
        <a href="/contact" class="button-secondary">Get in Touch</a>
      </div>
    </div>
  </div>
  
  <div class="hero-scroll">
    <div class="scroll-circle">
      <span class="scroll-arrow">↓</span>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 90vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: var(--space-8) 0;
  }
  
  .hero-title {
    font-size: clamp(2.5rem, 10vw, 6rem);
    line-height: 1.3;
    margin-bottom: var(--space-4);
    overflow: visible;
    padding: 0.1em 0;
  }
  
  .hero-line {
    display: block;
    transform: translateY(100%);
    opacity: 0;
    animation: slideUp 0.8s var(--transition-bounce) forwards;
  }
  
  @keyframes slideUp {
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }
  
  .hero-content {
    max-width: 800px;
    opacity: 0;
    transform: translateY(20px);
    animation: fadeIn 0.8s ease forwards;
    animation-delay: 0.6s;
  }
  
  @keyframes fadeIn {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .hero-description {
    font-size: 1.125rem;
    line-height: 1.6;
    margin-bottom: var(--space-5);
    color: var(--color-secondary);
  }
  
  .hero-buttons {
    display: flex;
    gap: var(--space-4);
    flex-wrap: wrap;
    margin-top: var(--space-7);
  }
  
  .hero-scroll {
    position: absolute;
    bottom: var(--space-6);
    left: 50%;
    transform: translateX(-50%);
    opacity: 0;
    animation: fadeIn 0.8s ease forwards;
    animation-delay: 1s;
  }
  
  .scroll-circle {
    width: 48px;
    height: 48px;
    border: 2px solid var(--color-primary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  
  .scroll-arrow {
    font-size: 1.5rem;
    animation: bounce 2s infinite;
  }
  
  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-6px);
    }
    60% {
      transform: translateY(-3px);
    }
  }
  
  @media (max-width: 768px) {
    .hero {
      padding: var(--space-9) 0 var(--space-6);
    }
    
    .hero-buttons {
      flex-direction: column;
      gap: var(--space-3);
    }
    
    .button, 
    .button-secondary {
      width: 100%;
      justify-content: center;
    }
    
    .hero-scroll {
      display: none;
    }
  }
</style>