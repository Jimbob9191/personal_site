---
---

<section class="contact-cta section">
  <div class="container">
    <div class="cta-content">
      <h2 class="cta-title fade-in">
        Let's Work Together
      </h2>
      <p class="cta-description fade-in">
        Have a project in mind? I'd be happy to hear from you. Get in touch and let's explore how we can bring your ideas to life.
      </p>
      <div class="cta-actions fade-in">
        <a href="/contact" class="button">Get in Touch</a>
      </div>
    </div>
  </div>
</section>

<style>
  .contact-cta {
    position: relative;
    background-color: var(--color-muted);
    padding: var(--space-8) 0;
    margin: 0;
    width: 100vw;
    left: 50%;
    right: 50%;
    margin-left: -50vw;
    margin-right: -50vw;
  }
  
  .cta-content {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }
  
  .cta-title {
    margin-bottom: var(--space-5);
    font-size: clamp(2rem, 5vw, 3.5rem);
  }
  
  .cta-description {
    font-size: 1.25rem;
    margin-bottom: var(--space-6);
    color: var(--color-secondary);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }
  
  .cta-actions {
    display: flex;
    justify-content: center;
  }
</style>